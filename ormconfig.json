{
    "type": "postgres",
    "host": "localhost",
    "port": 30101,
    "username": "sa",
    "password": "teste",
    "database": "db_inicial",
    "entities": [
        "./src/models/*.ts"
    ],
    "migrations": [
        "./src/database/migrations/*.ts"
    ],
    "cli": {
        "migrationsDir": "./src/database/migrations"
    }
}